//#include "main.h"
//#include "openmv.h"

//uint8_t mode_flag[10] = {0};

//void openmv_blue(void){
//    
//    HAL_UART_Receive(&huart1, (uint8_t *)&mode_flag, 10, HAL_MAX_DELAY); // 开始接收数据  
//    
//    mode_flag = 1;
//    while (1) {                
//        // 主循环，处理接收到的数据和执行其他任务（根据需要实现）  
//        
//        if (mode_flag == 1) {  
//            // 如果标志位为1，执行OpenMV的巡线模式  
//            // mode_flag == 1  
//        } else if (mode_flag == 2) {  
//            // 如果标志位为2，执行OpenMV的激光颜色识别模式  
//            // mode_flag == 2  
//        } else {  
//            // 其他情况，默认操作或重置模式  
//            // ... 默认操作 ... //  
//        }  
//    }  
//}
//    
//  
//void openmv_red(void){
//    
//    HAL_UART_Receive(&huart1, (uint8_t *)&mode_flag, sizeof(mode_flag), HAL_MAX_DELAY); // 开始接收数据  
////    mode_flag = 1;
//    while (1) {                
//        // 主循环，处理接收到的数据和执行其他任务（根据需要实现）  
//        
//        if (mode_flag == 1) {  
//            // 如果标志位为1，执行OpenMV的巡线模式  
//            // mode_flag == 1  
//        } else if (mode_flag == 2) {  
//            // 如果标志位为2，执行OpenMV的颜色识别模式  
//            // mode_flag == 2  
//        } else {  
//            // 其他情况，默认操作或重置模式  
//            // ... 默认操作 ... //  
//        }  
//    }  
//}
//    
    
    
    
    
    
    
    
    
    

    
    
    
    
    
    
    
    
    




























